export function getGithubProfileUrl(username) {
  return `https://github.com/${username}`;
}

export function getGithubRepoUrl(username, repository) {
  return `https://github.com/${username}/${repository}`;
}
